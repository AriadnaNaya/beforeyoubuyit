<!doctype html>
<html lang="en">

<head>
  <%- include('../partials/head') %>
</head>

<body>
  <!-- navbar header -->
  <%- include('../partials/navbar') %>

  <!-- Productos en Carrito de Compras -->


  <section class="container">
    <div class="col-12">
      <h4 class="title">Compra exitosa</h4>
    </div>
    <div method="GET" action="confirmacion" class="cart-wrapper">
      <% for(var i = 0; i < 5; i++) { %>
      <div class="in-cart w-100">
        <div class="col-12 col-md-2 img-wrapper"
          style="background-image: url('<%= carritoList[i].background_image %>');">
        </div>
        <div class="col-12 col-md-3">
          <p class="in-cart-title"><%=  carritoList[i].name%></p>
          <p class="bold-case">
            <% if ( carritoList[i].store === 'steam') { %>
            <i class="icon-steam"></i> Steam Key
            <% } %>
          </p>
          <p class="bold-case">
            <% if ( carritoList[i].store === 'epic-games') { %>
            <i class="icon-epic"></i> Epic Games Key
            <% } %>
          </p>
          <p class="bold-case">
            <% if ( carritoList[i].store === 'gog') { %>
            <i class="icon-gog"></i> GOG Key
            <% } %>
          </p>
          <p class="bold-case">
            <% if ( carritoList[i].store === 'xbox-store') { %>
            <i class="icon-xbox"></i> Xbox Store Key
            <% } %>
          </p>

          <p class="bold-case mb-3">Activación global</p>
          <a href="/" class="text-secondary">Seguir comprando</a>
        </div>
        <div class="col-12 col-md-7 text-small">
          <h4 class="bold-case mb-0"><%= carritoList[i].key %></h4>
          <small class="lighten">Código de activación</small>
          <p class="mt-3">Sigue estas instrucciones para activar una copia física o digital de un juego en <%= carritoList[i].store %>:</p>
          <ol>
            <li>
              Abre el cliente de <%= carritoList[i].store %> e inicia sesión en tu cuenta.
            </li>
            <li>
              Haz clic en el menú Juegos.
            </li>
            <li>
              Selecciona Activar un producto en <%= carritoList[i].store %>.
            </li>
            <li>
              Sigue las instrucciones en pantalla para completar el proceso.
            </li>
          </ol>
          <p class="mt-3">Consulta el artículo Claves de producto de copias físicas o digitales para obtener más
            información sobre el
          registro de claves.</p>
          </div>
        </div>
      <% } %>
    </div>
    <div class="cta row">
      <div class="col-12">
        <p><strong>Importante:</strong></p>
        <p>Asegúrate de que activas el juego a través de la aplicación de tu respectivo Store. El sitio web de Steam no puede utilizarse para activar un juego. Si aún no lo has hecho, puedes descargarte e instalar la aplicación de Steam haciendo clic en el botón verde "Instalar Steam ahora" que encontrarás aquí: <a class="text-secondary" href="javascript:void(0)">http://store.steampowered.com/about/</a></p>
      </div>
    </div>
  </section>

  <%- include('../partials/footer') %>

  <%- include('../partials/scripts') %>
</body>

</html>